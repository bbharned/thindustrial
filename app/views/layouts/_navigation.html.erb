<nav class="navbar navbar-default">
  <div class="container-fluid">
  <div class="content-box">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to (image_tag 'Thindustrial17-Logo_600x301.png'), root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <!--<div id="nav-center-it">-->
      
      <ul class="nav navbar-nav navbar-right goAway">
      <% if logged_in? %>
        <%= link_to 'LOGOUT', logout_path, type: "button", method: :delete, class: "btn btn-default navbar-btn login-button" %> 
      <% else %>
        <%= link_to 'LOGIN', login_path, type: "button", class: "btn btn-default navbar-btn login-button" %>
      <% end %>      
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'HOME', root_path %></li>
        <% if logged_in? && !current_user.admin? %>
        <li>
          <%= link_to 'MY PROFILE', user_path(current_user) %>
        </li>
        <% elsif  logged_in? && current_user.admin? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">ADMIN <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'View Attendees', users_path %></li>
            <!--<li role="separator" class="divider"></li>-->
            <li><%= link_to 'Edit Courses', courses_path %></li>
            <li><%= link_to 'View Class Registrations', schedules_path %></li>
            <li><%= link_to 'View/Edit My Profile', user_path(current_user) %></li>
          </ul>
        </li>
        <% else %>
        <li>
          <%= link_to 'REGISTER', register_path %>
        </li>
        <% end %>
        <li><%= link_to 'SPONSORS & EXHIBITORS', sponsors_path %></li>
        <li>
        <% if logged_in? %>
          <%= link_to 'COURSES', courses_path %>
        <% else %> 
          <%= link_to 'AGENDA', agenda_path %>
        <% end %>
        </li>
        <li><%= link_to 'CONTACT', contact_path %></li>
        
      </ul>
      <!--</div>-->
      <ul class="nav navbar-nav navbar-right appear">
      <% if logged_in? %>
        <%= link_to 'LOGOUT', logout_path, type: "button", method: :delete, class: "btn btn-default navbar-btn login-button" %> 
      <% else %>
        <%= link_to 'LOGIN', login_path, type: "button", class: "btn btn-default navbar-btn login-button" %>
      <% end %>
      </ul>
      
    </div><!-- /.navbar-collapse -->
  </div>
  </div><!-- /.container-fluid -->
</nav>